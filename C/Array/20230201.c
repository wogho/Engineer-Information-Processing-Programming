// 1. 입력값이 54321일 경우 출력값이 43215로 출력되어야 한다.
// 배열 입력, 인덱스 접근(빈칸 문제)

#include <stdio.h>

int main(void) {
    int n[5];
    int i;

    for (i = 0; i < 5; i++) {
        printf("숫자를 입력해주세요 : ");
        scanf("%d", &n[i]);
    }

    for (i = 0; i < 5; i++) {
        printf("%d", (            ));
    }

    return 0;
}
